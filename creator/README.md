# Creator context

These files contain persistent, human-approved context shared across runs. Start sparse, use `$content-flow-setup` to improve them, and commit only information suitable for Git. Distinguish creator statements from observed sample patterns and assumptions. Run-specific opinions belong in `runs/`, not here.

